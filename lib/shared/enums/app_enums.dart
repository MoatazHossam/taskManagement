enum AppLanguage { arabic, english }

enum AppThemePreference { light, dark, system }

enum DemoUserRole { employee, manager, seniorManagement, administrator }

enum SimulatedConnectivityStatus { online, offline, unstable }

enum AuthenticationStatus {
  initializing,
  unauthenticated,
  selectingProfile,
  authenticating,
  authenticated,
  locked,
  expired,
  failure,
}

enum AppBreakpoint { compactPhone, largePhone, tablet }
